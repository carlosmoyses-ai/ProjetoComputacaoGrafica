// ---------------------------------------------------------------------------

#ifndef uRetaBresenhamH
#define uRetaBresenhamH
// ---------------------------------------------------------------------------
#endif

class Bresenham {
public:
	double px0, px1, py0, py1, pr, ax0, ax1, ay0, ay1, pr;

	Bresenham();
	Bresenham(double x0, double y0, double x1, double y1);
	Bresenham(double xc, double yc, double r);
	void desenhaReta(TCanvas * canvas);
	void desenhaCirc(TCanvas * canvas);
};
